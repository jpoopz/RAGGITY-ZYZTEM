# Python Version: 3.11+ recommended (tested on 3.11, 3.12, 3.14)
# Note: Python 3.14 is currently alpha/beta. For production use, Python 3.11 or 3.12 is recommended.

# === Core Dependencies ===
requests>=2.31.0
pyyaml>=6.0.1
psutil>=5.9.0
numpy>=1.24.0
packaging>=23.0  # For version comparison in diagnostics

# === API Backend (FastAPI primary; Flask optional — either-or is fine) ===
# FastAPI stack for rag_api.py
fastapi>=0.110.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Optional Flask stack (kept for either-or compatibility)
flask>=3.0.0
flask-cors>=4.0.0

# === Vector Stores ===
# FAISS is the default vector store (no C++ compiler needed on Windows)
faiss-cpu>=1.7.4

# Optional: ChromaDB alternative (Windows often needs build tools) — keep optional
# chromadb>=0.4.22

# === Document Processing ===
pypdf>=3.0.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
tqdm>=4.66.0

# === LLM Integration ===
# openai>=1.0.0  # Only needed if provider=openai
ollama>=0.1.0

# === UI ===
customtkinter>=5.2.0
Pillow>=10.0.0

# === Optional: GPU Monitoring ===
# pynvml>=11.5.0  # Only needed for NVIDIA GPU monitoring

# === Optional: Web Retrieval ===
# duckduckgo-search>=3.9.0
# newspaper3k>=0.2.8

# === Optional: CLO 3D Module ===
# trimesh>=3.20.0
# open3d>=0.18.0
# torch>=2.0.0

# === Optional: Voice Control ===
# pyaudio>=0.2.11
# keyboard>=0.13.5
# pygame>=2.5.0

# === Security ===
cryptography>=41.0.0

# === Academic Search & Citations ===
# ArXiv feed parsing (lightweight, no arxiv package needed)
feedparser>=6.0.0
# Citation processing
citeproc-py>=0.8.1
lxml>=5.3.0
pybtex>=0.25.1
# Ranking algorithms
rank-bm25>=0.2.2
# Embeddings for dense retrieval (optional)
# sentence-transformers>=3.0.1
# Data processing
# pandas>=2.2.0

# === PDF Processing ===
# GROBID client for structured PDF extraction (optional)
# grobid-client-python>=0.0.8

# === Development ===
pre-commit>=3.5.0
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.24.0
black>=24.4.0
isort>=5.13.0
flake8>=7.0.0
pydantic>=2.0.0
loguru
